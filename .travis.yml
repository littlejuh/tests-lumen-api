language: php

php:
  - 5.6
  - 7.0
  - hhvm
  - nightly

before_script:
  - composer self-update
  - composer install
  - touch database/database.sqlite
  - sed -i '1s/^/DB_CONNECTION=sqlite\n/' .env

script:
 - vendor/bin/phpunit
